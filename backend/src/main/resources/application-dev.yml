spring:
  datasource:
    password: ${DATABASE_PASSWORD:cooperative-dev-only}
  flyway:
    validate-on-migrate: false
app:
  jwt-secret: ${JWT_SECRET:development-only-jwt-secret-32-bytes-minimum-change-me}
  encryption-key: ${ENCRYPTION_KEY:ZGV2ZWxvcG1lbnQta2V5LTMyLWJ5dGVzLWxvbmcxMjM=}
  dev-auth: true
  sandbox-payments: true
  demo-mode: ${DEMO_MODE:true}
  demo-reset-token: ${DEMO_RESET_TOKEN:cooperative-demo-reset-2026}
